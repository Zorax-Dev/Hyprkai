# ---------- Background ----------
background #000000
background_opacity 0.8

# ---------- Padding ----------
window_padding_width 12

# ---------- Dynamic padding (closest equivalent) ----------
# Kitty doesn’t have "dynamic_padding" exactly like Alacritty,
# but this keeps things visually consistent.
placement_strategy center

# ---------- Performance / sanity ----------
repaint_delay 10
input_delay 3
sync_to_monitor yes

